<% if signed_in? %>

  <table class="front" summary="For signed-in users">
    <tr>
      <td class="main">
        <h1 class="micropost">What's up?</h1>
      </td>
      <td class="sidebar round">
        <%= render 'shared/user_info' %>
        <%= render 'shared/stats' %>
      </td>
    </tr>
  </table>

<% else %>

<h1>Viarnes Hub</h1>
<p>
  This is the home page for the Viarnes Family and all its wonderful members. Here they can share experiences and media of all sorts with out the hassle of corporate social media.
</p>

<%= link_to "Sign up now!", signup_path, :class => "signup_button round" %>

<% end %>